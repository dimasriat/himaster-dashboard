<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Dashboard</title>
		<link rel="stylesheet" href="/assets/css/style.css" />
	</head>
	<body>
		<div class="wrapper">
			<%- include('components/header', {title: 'Update Todo'}); %>
			<%- include('components/nav'); %>
			<section>
				<form action="/update/<%= data.id %>" method="POST">
					<p><input type="text" name="title" placeholder="title..." value="<%= data.title %>"/></p>
					<p><textarea name="body" placeholder="body..."><%= data.body %></textarea></p>
					<input type="submit" value="update!" />
				</form>
			</section>
			<% for (err of error) { %> 
				<div class="err">
					<%= err.msg %>
				</div>
			<% }; %>
		</div>
	</body>
</html>
