<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Dashboard</title>
		<link rel="stylesheet" href="/assets/css/init.css" />
		<link rel="stylesheet" href="/assets/css/index.css" />
	</head>

	<body>
		<main>
			<header>
				<h1>Login Page</h1>
			</header>
			<nav>
				<a href="/">home</a>
				<a href="/login">login</a>
				<a href="/register">register</a>
			</nav>
			<article>
				<h2>Medium</h2>
				<p>
					Selamat datang di website penambah ilmu pengetahuanmu.
					Silakan berbagi kisah sesama para programmer untuk saling
					menyemangati!
				</p>
			</article>
			<% for (post of posts) { %>
			<section>
				<figure>
					<% if (post.user.avatar) { %>
						<div class="fakeava" style="background-image: url('/assets/upload/<%=post.user.avatar%>');"></div>
						<% } else { %>
						<div class="fakeava"></div>
						<% } %>
					<figcaption>
						<b><%= post.user.username %></b>
						<div>
							<h2><%= post.title %></h2>
							<p>
								<%= post.body %>
							</p>
						</div>
					</figcaption>
				</figure>
			</section>
			<% } %>
		</main>
	</body>
</html>
